# Google Cloud Project ID
GCP_PROJECT=your-project-id-here

# Vertex AI Location (default: us-central1)
GOOGLE_CLOUD_LOCATION=us-central1

# Local Mode (Optional: If set, uses Free Gemini API + Mock DB instead of Cloud)
GEMINI_API_KEY=your_api_key_here

# Service Account Key (Optional, for local dev if not using gcloud auth)
# GOOGLE_APPLICATION_CREDENTIALS="path/to/service-account.json"

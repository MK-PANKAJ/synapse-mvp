name: synapse_frontend
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^1.2.0
  google_fonts: ^6.1.0
  flutter_markdown: ^0.6.14
  youtube_player_flutter: ^9.0.0
  provider: ^6.0.0 # Added provider for state management if needed

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
